import React from 'react';
import {Container, Row} from 'react-bootstrap';
import {observer} from 'mobx-react-lite';

const PData = observer( () => {
    return (
        <Container>
            <Row>
                
            </Row>
            <Row>

            </Row>
        </Container>
    );
});

export default PData;