import React from 'react';
import {Container} from 'react-bootstrap';
import {observer} from 'mobx-react-lite';

const PRoles = observer( () => {
    return (
        <Container></Container>
    );
});

export default PRoles;